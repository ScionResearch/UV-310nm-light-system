Hardware: RPi 4b 8GB RAM + DS1307 based RTC module + DF Robot 7" LCD display model DFR0678 + 64GB SD card
Pi image: Debian Bullseye (Legacy, 64-bit) Full (Fisrt orange unit), Debian Bookworm (Legacy, 64-bit) Full (Newer blue unit)

Username: admin
Password: andover1221!
Hostname: uv-control

-------------------------------------------------------------------------------
                     --- Software packages installed ---
sudo raspi-config
Turn on I2C and SPI for older (orange) unit, I2C and Serial for newer (blue) unit - disable serial terminal over UART and enable UART.
reboot

--------

Virtual keyboard (older unit only!):
sudo apt install onboard

Note: don't use autohide - it breaks the keyboard, instead use start minimised

--------

Node Red:
bash <(curl -sL https://raw.githubusercontent.com/node-red/linux-installers/master/deb/update-nodejs-and-nodered)

--------

Node Red settings changes:
nano .node-red/settings.js

Add configuration page url:
httpAdminRoot: '/config',

Set context storage to local file system:
contextStorage: {
    default: {
        module:"localfilesystem"
    },
},

Change dashboard UI URL to site root:
ui: { path: "/" },

Save and exit

Set node-red to run on startup
sudo systemctl enable nodered
sudo systemctl start nodered

--------

RTC setup:

sudo i2cdetect -y 1
Check that the RTC is present on I2C bus (address 68 for DS1307 module)

Edit the boot file:
sudo nano /boot/firmware/config.txt

Add line to end of file:
dtoverlay=i2c-rtc,ds1307

Save and reboot

Disable fake HW clock:
sudo apt-get -y remove fake-hwclock
sudo update-rc.d -f fake-hwclock remove
sudo systemctl disable fake-hwclock

Edit HW clock script:
sudo nano /lib/udev/hwclock-set

comment out # these lines:
#if [ -e /run/systemd/system ] ; then
#    exit 0
#fi

#/sbin/hwclock --rtc=$dev --systz

Sync time from Pi to RTC:
sudo hwclock -r

Check date:
date

Source - https://learn.adafruit.com/adding-a-real-time-clock-to-raspberry-pi/set-rtc-time

--------

NodeRed setup:
Connect unit to the network
Browse to http://uv-control.scion.local:1880/config
Go to Import and paste in the relevant flow (UVControlFlow_xx-xx-xxxx.json)

NodeRed should install missing libraries, if not install them manually
Reboot to start dashboard properly

sudo reboot

--------

Setup desktop to run chromium in kiosk mode:

Debian Bookworm onwards ---------->

Create autostart dir:
mkdir -p ~/.config/autostart

Create app file:
nano ~/.config/autostart/chromium.desktop

Paste in:

[Desktop Entry]
Encoding=UTF-8
Version=0.9.4
Type=Application
Name=Chromium
Comment=Chromium in kiosk mode for UV controller UI display
Exec=chromium-browser --kiosk http://localhost:1880
Terminal=false
Hidden=false
Categories=Utility;

Close and save

Change file permissions:
chmod 644 ~/.config/autostart/chromium.desktop

Restart and test

sudo reboot

When the keyring password prompt appears, save with no password and accept the unencrypted keyring warning.

---------------------------------|

Or - using bashrc script (runs on every login so you will get multiple instances if SSH connecting in!)
sudo nano ~/.bashrc

Add to the bottom of the file:

echo Running at boot
DISPLAY=:0.0 chromium-browser --kiosk

Close and save

sudo reboot

----------------------------------|

Older Pi OS versions ------------->
Create autostart file in user home directory:
mkdir /home/admin/.config/lxsession
mkdir /home/admin/.config/lxsession/LXDE-pi
cp /etc/xdg/lxsession/LXDE-pi/autostart /home/admin/.config/lxsession/LXDE-pi/

Edit to add chromium to autostart:
nano /home/admin/.config/lxsession/LXDE-pi/autostart

Add line:
@chromium-browser --kiosk

Source - https://forums.raspberrypi.com/viewtopic.php?t=294014

--------

